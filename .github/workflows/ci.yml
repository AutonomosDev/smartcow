# GitHub Actions — smartcow
# Quality gates: typecheck + tests on PRs and pushes to main
# Deployment: SSH + git pull + docker compose on Hostinger VPS (push to main only)

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  typecheck:
    name: Typecheck
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      # AUT-284 — Drift check: si el schema TS diverge de las migrations
      # SQL, drizzle-kit generate produce un nuevo archivo. Si lo produce en
      # CI → el dev olvidó correr generate → PR rojo.
      - name: Migration drift check
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL_CI || 'postgres://user:pass@localhost:5432/ci' }}
        run: |
          npx drizzle-kit generate --name=ci_drift_check || true
          if [ -n "$(git status --porcelain src/db/migrations/ src/db/migrations/meta/)" ]; then
            echo "❌ Schema drift detectado — src/db/schema/*.ts diverge de migrations/*.sql"
            echo "   Corre: npx drizzle-kit generate --name=<descripción>"
            git status --porcelain src/db/migrations/ src/db/migrations/meta/
            git diff src/db/migrations/ src/db/migrations/meta/ | head -200
            exit 1
          fi
          echo "✓ Schema TS === migrations SQL"

  test:
    name: Tests
    runs-on: ubuntu-latest
    needs: typecheck
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test --if-present

  deploy:
    name: Deploy to VPS
    runs-on: ubuntu-latest
    needs: [typecheck, test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    concurrency:
      group: deploy-prod
      cancel-in-progress: false
    steps:
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H "${{ secrets.VPS_HOST }}" >> ~/.ssh/known_hosts

      - name: Deploy (git pull + rebuild + restart)
        run: |
          ssh -i ~/.ssh/id_ed25519 "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" bash -s << 'EOF'
            set -euo pipefail
            cd /var/www/smartcow
            echo "→ git pull"
            git pull origin main
            echo "→ docker build"
            docker compose build app
            echo "→ docker up"
            docker compose up -d app
            echo "→ health"
            sleep 3
            docker compose ps
          EOF

      - name: Smoke test
        run: |
          sleep 5
          code=$(curl -sSo /dev/null -w "%{http_code}" https://smartcow.cl/login)
          echo "smartcow.cl/login → $code"
          [ "$code" = "200" ] || exit 1
